all:
  vars:
    ansible_user: ansible
  children:
    control_node:
      hosts:
        LPRANSIBLE01:
          ansible_host: 192.168.10.120
    # webservers: a completer quand une VM web sera provisionnee via Terraform.
    # Utiliser webserver.yml pour appliquer le role apache a ce groupe.
