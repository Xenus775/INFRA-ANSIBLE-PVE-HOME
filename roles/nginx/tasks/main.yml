---
- name: Installer nginx
  ansible.builtin.apt:
    name: "{{ nginx_packages }}"
    state: present
    update_cache: true
    cache_valid_time: 3600

- name: Activer et demarrer nginx
  ansible.builtin.systemd:
    name: nginx
    enabled: true
    state: started
